let x = 3.14;
let y = 5;
let z = 9;
let maximum;
let minimum;

// x = Math.round(x); // returns the nearest integer.
// x = Math.floor(x); // returns the value of x rounded down to its nearest integer.
// x = Math.ceil(x); // returns the value of x rounded up to its nearest integer.
// x = Math.pow(x, 2); // returns the value of x to the power of y.
// x = Math.sqrt(x); // returns the value of x to the power of y.
// x = Math.abs(x); // returns the absolute (positive) value of x.
// maximum = Math.max(x, y, z); // can be used to find the lowest or highest value in a list of arguments.
minimum = Math.min(x, y, z); // can be used to find the lowest or highest value in a list of arguments.

console.log(minimum);
