let fruits = ["banana", "apple", "orange", "mango"];

// fruits = fruits.sort(); // sort in alphabetical order.
fruits = fruits.sort().reverse(); // sort in reverse alphabetical order.

for (let fruit of fruits) {
  console.log(fruit);
}
